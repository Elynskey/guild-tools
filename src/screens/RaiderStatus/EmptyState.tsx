export function EmptyState() {
  return (
    <div style={{ padding: 48, textAlign: 'center', border: '1px dashed var(--border-hairline)', borderRadius: 'var(--radius-md)', color: 'var(--text-muted)' }}>
      No raiders match that filter.
    </div>
  );
}
